<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Hesabınızı Doğrulayın</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="d-flex justify-content-center align-items-center vh-100 bg-light">
    <form action="/resend-verification" method="POST" class="p-4 bg-white rounded shadow">
        <h4>Verify Your Account</h4>
        <p><%= email %> Click the button to verify your account with address.</p>
        <input type="hidden" name="email" value="<%= email %>">
        <button type="submit" class="btn btn-primary">E‑postayı Tekrar Gönder</button>
    </form>
    <h2><%= email %> A verification email has been sent to your address.</h2>
    <% if (typeof message !== 'undefined') { %>
        <p><%= message %></p>
    <% } %>

</body>
</html>
