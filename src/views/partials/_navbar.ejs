<nav class="navbar navbar-expand-lg bg-dark navbar-dark">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link <%= page_name === 'home' ? 'active' : ''%>" aria-current="page" href="/">Home &#127968;</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <%= page_name === 'dashboard' ? 'active' : ''%>" href="/dashboard">Dashboard &#11035;</a>
                </li>

                <% if (!token) { %>

                    <li class="nav-item">
                        <a class="nav-link <%= page_name === 'login' ? 'active' : ''%>" href="/login">Login
                            &#x1F6E1;</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= page_name === 'register' ? 'active' : ''%>" href="/register">Register
                            &#x1F6E1;</a>
                    </li>

                    <% } %>

                            <li class="nav-item">
                                <a class="nav-link <%= page_name === 'contact' ? 'active' : ''%>"
                                    href="/contact">Contact
                                    &#x2709;</a>

                            </li>

                            <% if (token) { %>

                                <li class="nav-item">
                                    <a class="nav-link" aria-disabled="true" href="/logout">Logout &#x21AA;</a>
                                </li>

                                <% } %>

                                        <% if (idAdmin) { %>
                                            <li class="nav-item">
                                                <a href="#" class="nav-link"
                                                    onclick="openAdminPanel(); return false;">Admin Panel &#x1F9D1;&#x200D;&#x1F4BB;</a>
                                            </li>
                                            <% } %>

            </ul>

        </div>
        <a class="navbar-brand" href="/">Product Brand İnformation System</a>
    </div>
</nav>

<script>
function openAdminPanel() {
    const w = screen.availWidth;
    const h = screen.availHeight;

    const features = [
        `width=${w}`,
        `height=${h}`,
        `left=0`,
        `top=0`,
        'toolbar=0',
        'menubar=0',
        'location=0',
        'status=0',
        'scrollbars=1',
        'resizable=1'
    ].join(',');

    const win = window.open('/admin', 'AdminPanel', features);

    if (win) {
        win.moveTo(0, 0);
        win.resizeTo(w, h);
        win.focus();
    } else {
        alert("The browser blocked the pop-up.");
    }
}
</script>
